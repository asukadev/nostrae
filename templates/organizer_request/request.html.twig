{% extends 'base.html.twig' %}

{% block title %}Devenir organisateur{% endblock %}

{% block body %}
<div class="container mt-5">
<h2 class="h2 d-inline-flex align-items-center">Devenir Organisateur</h2>

    <p class="lead">
        Remplissez ce formulaire pour soumettre votre demande. Un administrateur étudiera votre candidature.
    </p>

    {{ form_start(form) }}
        <div class="mb-3">
            {{ form_label(form.motivation) }}
            {{ form_widget(form.motivation, {'attr': {'class': 'form-control', 'placeholder': 'Expliquez pourquoi vous souhaitez devenir organisateur...'}}) }}
            {{ form_errors(form.motivation) }}
        </div>

        <button class="btn btn-primary mt-3">
            Envoyer la demande
        </button>
    {{ form_end(form) }}
</div>
{% endblock %}
